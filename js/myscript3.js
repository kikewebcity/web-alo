
function imageGallery() {
	const highlight = document.querySelector(".gallery-highlight");
	const previews = document.querySelectorAll(".room-preview img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
} 
imageGallery(); 
/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/
function imageGallery2() {
	const highlight2 = document.querySelector(".gallery-highlight2");
	const previews = document.querySelectorAll(".room-preview2 img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight2.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
}

imageGallery2(); 
/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/


function imageGallery3() {
	const highlight3 = document.querySelector(".gallery-highlight3");
	const previews = document.querySelectorAll(".room-preview3 img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight3.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
}

imageGallery3(); 
/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/

/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/


function imageGallery4() {
	const highlight4 = document.querySelector(".gallery-highlight4");
	const previews = document.querySelectorAll(".room-preview4 img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight4.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
}

imageGallery4(); 


/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/


function imageGallery5() {
	const highlight5 = document.querySelector(".gallery-highlight5");
	const previews = document.querySelectorAll(".room-preview5 img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight5.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
}


imageGallery5(); 


/* Funciones JavaScript
   Versión 0.1
   Autor: César Krall
   Curso: Tutorial básico del programador web: JavaScript desde cero
*/


function imageGallery6() {
	const highlight6 = document.querySelector(".gallery-highlight6");
	const previews = document.querySelectorAll(".room-preview6 img ");
	

	previews.forEach(preview => {
		preview.addEventListener("click", function()  {
        const smallSrc = this.src;
        const bigScr = smallSrc.replace("small", "big");
        highlight6.src = bigScr;
        preview.forEach(preview => preview.classList.remove("room-active"));
        preview.classList.add('room-active');
          
		});
	});
}

imageGallery6(); 